package hardcoded.geometrize;

public class Test {
	/**
	 * JNI?
	 * 
	 * 
	 * Create 4 new shape types
	 * {
	 *     LINE_SOFT_HALO,
	 *     LINE_CIRCLE,
	 *     LINE_STRONG_HALO,
	 *     LINE_SQUARE,
	 * }
	 * 
	 * Restrict sizes to 6 levels.   TODO: Get the exact values!
	 * Restrict opacity to 6 levels. TODO: Get the exact values!
	 * 
	 * Restrict the colors to those given by RustGUIAnalyser.
	 * 
	 * bestRandomState should look though all posibilities.
	 */
}
